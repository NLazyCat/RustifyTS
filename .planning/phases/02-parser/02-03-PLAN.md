---
phase: 02
plan: 03
type: wave
autonomous: true
wave: 3
depends_on: [02-02]
objective: Implement AST Types and Node Infrastructure for TypeScript parser
---

# Plan 02-03: AST Types and Node Infrastructure

**Phase:** 02-parser
**Wave:** 3
**Status:** Ready for execution
**Generated:** 2026-03-01

## Objective

Implement AST types and node infrastructure for the TypeScript parser. This includes:
1. Creating categorized AST type definitions for all TypeScript constructs
2. Implementing AstNode with arena allocation using bumpalo
3. Creating NodeBuilder for safe node construction
4. Writing unit tests for arena allocation and node structure

## Context

References:
- @.planning/phases/02-parser/02-CONTEXT.md
- @.planning/phases/02-parser/02-RESEARCH.md
- @.planning/phases/02-parser/PLAN.md

## Dependencies

- **Wave 2:** Span and Location Tracking (MISSING - will be handled as deviation)
- Cargo.toml dependencies already added in Wave 1
- src/parser/error.rs already created in Wave 1

## Success Criteria

- [ ] All tasks completed successfully
- [ ] src/parser/ast/types.rs defines all core TypeScript node types
- [ ] src/parser/ast/node.rs implements AstNode with arena lifetime
- [ ] bumpalo::Bump is integrated for node allocation
- [ ] NodeBuilder provides safe node construction interface
- [ ] Unit tests verify arena allocation correctness
- [ ] All tests pass
- [ ] Cargo build succeeds without warnings

## Tasks

<task id="1" type="auto">
<description>Create src/parser/ast/types.rs with categorized enums for TypeScript AST nodes (Statement, Expression, Declaration, Pattern, Type, Literal)</description>
<command>Create types.rs with enum definitions covering let/const declarations, function declarations, if/while statements, arithmetic and logical expressions, string/number literals, and type annotations</command>
<verification>All node types are defined and properly structured</verification>
</task>

<task id="2" type="auto">
<description>Create src/parser/ast/node.rs with AstNode struct containing NodeKind, optional Span, and arena-based lifetime</description>
<command>Create node.rs with bumpalo::Bump integration, AstNode<'a> struct with NodeKind, optional span, and methods for accessing children. Implement NodeBuilder for constructing nodes in an arena.</command>
<verification>AstNode uses arena lifetime correctly, NodeBuilder allocates nodes in bumpalo arena</verification>
</task>

<task id="3" type="auto">
<description>Create src/parser/ast/mod.rs with module declarations for span, types, node, visitor</description>
<command>Create mod.rs with pub mod span, types, node, and appropriate re-exports. Handle missing span.rs gracefully.</command>
<verification>Module structure is correct, types are accessible</verification>
</task>

<task id="4" type="auto">
<description>Add unit tests for node.rs: test arena allocation, node building, span assignment, and structural equality</description>
<command>Add #[cfg(test)] module in node.rs with comprehensive tests</command>
<verification>All node tests pass</verification>
</task>

<task id="5" type="auto">
<description>Update src/parser/mod.rs to declare ast module and re-export key types</description>
<command>Edit src/parser/mod.rs to include pub mod ast and re-export AstNode, NodeKind, Span if available</command>
<verification>Parser module exposes AST types</verification>
</task>

## Output Specification

### Files Created
- src/parser/ast/types.rs
- src/parser/ast/node.rs
- src/parser/ast/mod.rs

### Files Modified
- src/parser/mod.rs

### Tests Added
- Unit tests in node.rs for arena allocation
- Tests verify node structure and lifetime correctness

## Deviations Expected

- **Wave 2 Missing:** Span and Location Tracking not completed. Will create a minimal Span type or handle as optional.
- May need to create stub span.rs module before node.rs can compile.

## Notes

- Use bumpalo for arena allocation to enable efficient AST storage
- AST nodes use lifetime tied to the arena
- Span tracking is optional (cfg feature) per CONTEXT.md decisions
- Node immutability ensured after construction (no setter methods)

---
